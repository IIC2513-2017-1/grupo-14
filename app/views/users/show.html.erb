<%if current_user%>
	<p id="notice"><%= notice %></p>

	<p>
	  <strong>Name:</strong>
	  <%= @user.name %>
	</p>

	<p>
	  <strong>Mail:</strong>
	  <%= @user.mail %>
	</p>
	<% if is_admin %>
		<p>
		  <strong>Role:</strong>
		  <%= @user.role %>
		</p>
	<% end %>
	<p>
		<strong>Friends:</strong>
		<ul>
			<% @user.friends.each do |friend| %>
			<% friend.name %>
			<% end %>
		</ul>
	</p>

	<p>
		<strong>Pending friend requests:</strong>
		<ul>
			<% @user.request_targets.each do |target| %>
			<% target.name %>
			<% end %>
		</ul>
	</p>

	<p>
		<strong>Received friend requests:</strong>
		<ul>
			<% @user.request_senders.each do |sender| %>
			<% sender.name %>
			<% end %>
		</ul>
	</p>

	<% if not current_user.friends.include?(@user) %>
		<p>
			<a href="#">Send friend request</a>
		</p>
	<% end %>

	<%= link_to 'Edit', edit_user_path(@user) if @user == current_user or is_admin %> |
	<%= link_to 'Back', users_path %>
<%end%>
